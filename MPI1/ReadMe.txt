========================================================================
    CONSOLE APPLICATION : MPI K-Means Running App
========================================================================

This application runs a K-means algorithm using MPI.

Example data set is in data.txt